@{
    Layout = null;
}

<!DOCTYPE html>
<html>
<head lang="en">
    <meta charset="UTF-8">
    <title>看视频赢大奖</title>
    <meta name="viewport" content="width=750, user-scalable=no, target-densitydpi=device-dpi" />
    <meta name="apple-mobile-web-app-capable" content="yes" />
    <meta name="apple-mobile-web-app-status-bar-style" content="black" />
    <meta name="format-detection" content="telephone=no">
    <link href="~/Content/yamaha/css/style.css" rel="stylesheet" type="text/css" />
    <script src="http://code.jquery.com/jquery-1.8.3.min.js"></script>
    <script src="~/Content/yamaha/js/main.js"></script>
    <script>
        $(function () {
            var q1img = "<img src='/Content/yamaha/image/Q1.png' class='q1'>"
            var q2img = "<img src='/Content/yamaha/image/Q2.png' class='q1'>"
            var q3img = "<img src='/Content/yamaha/image/Q3.png' class='q1'>"
            var cookiedata = checkCookie();
           
            if (cookiedata.indexOf(",") == -1) {
                $("body").append(q3img);
            } else {
                var arr3 = cookiedata.split(",");
                var arr3lenth = arr3.length;
                if (arr3lenth == 3) {
                    $("body").append(q1img);
                } else if (arr3lenth == 2) {
                    $("body").append(q2img);
                } else {

                }

            }

          $("form .nextbtn").on("click", function () {
                var checkval = $("form input[type=radio]:checked").val();
       
                if (checkval == null || checkval == ""){
                    alertdialog1(str.chooseanser);
                    return;

                } else {
                    if (checkval != "C") {
                        // setCookie("errorcount", 1, 0);
                        alertdialog3(str.erroranser);
                    } else {
                        var cookiedata = checkCookie();
                        if (cookiedata.indexOf(",") != -1) {
                            var arr3 = cookiedata.split(",");

                            if (arr3.length > 0) {
                                console.log(arr3);
                                var arrti1 = arr3.shift();
                                // alert(arr3);
                                setCookie("cookiename", arr3, 365);
                                location.href = "/Yamaha1/" + arr3[0];

                            }
                        } else {

                            alertdialog2(str.complete);

                        }
                    }
                        //setCookie("cookiet1", checkval, 365);
                   
                }
            })
           

        })
    </script>
</head>
<body>
    <div class="t7wrap">
        <video src="http://hebaihe.oss-cn-shanghai.aliyuncs.com/video/0627%E9%9B%85%E9%A9%AC%E5%93%8830%E7%A7%92%E6%88%90%E7%89%871.mp4" controls="controls" class="video" width="100%" height="100%" controls poster="~/Content/yamaha/image/videobg.jpg">
            Your browser does not support the video tag.
        </video>
        <p class="title">马克西姆觉得电钢琴的触键感觉和什么一模一样?</p>
        <form>
            <input type="radio" id="t7a" value="A" name="t7"><label for="t7a" class="t7a"></label>
            <input type="radio" id="t7b" value="B" name="t7"><label for="t7b" class="t7b"></label>
            <input type="radio" id="t7c" value="C" name="t7"><label for="t7c" class="t7c"></label>
            <input type="button" class="nextbtn">
        </form>
    </div>
    <img src="~/Content/yamaha/image/rulebtn.png" class="rule" />

    <img src="~/Content/yamaha/image/closerule.png" class="closebtn" />
</body>
</html>
