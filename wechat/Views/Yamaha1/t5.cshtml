@{
    Layout = null;
}

<!DOCTYPE html>
<html>
<head lang="en">
    <meta charset="UTF-8">
    <title>题目问答</title>
    <meta name="viewport" content="width=750, user-scalable=no, target-densitydpi=device-dpi" />
    <meta name="apple-mobile-web-app-capable" content="yes" />
    <meta name="apple-mobile-web-app-status-bar-style" content="black" />
    <meta name="format-detection" content="telephone=no">
    <link href="~/Content/yamaha/css/style.css" rel="stylesheet" type="text/css" />
    <script src="http://code.jquery.com/jquery-1.8.3.min.js"></script>
    <script src="~/Content/yamaha/js/main.js"></script>
    <script>
        $(function(){
            var q1img = "<img src='/Content/yamaha/image/Q1.png' class='q1'>"
            var q2img = "<img src='/Content/yamaha/image/Q2.png' class='q1'>"
            var q3img = "<img src='/Content/yamaha/image/Q3.png' class='q1'>"
            var cookiedata = checkCookie();
            if (cookiedata.indexOf(',') != -1) {
                var arr3 = cookiedata.split(",");
                arr3leng = arr3.length;
                if (arr3leng == 3) {
                    $(".t1wrap").append(q1img);
                } else if (arr3leng == 2) {
                    $(".t1wrap").append(q2img);
                } else if (arr3leng == 1) {
                    $(".t1wrap").append(q3img);
                } else {
                    return;
                }
            }

            $("form .nextbtn").on("click",function(){
                var checkval=$("form input[type=radio]:checked").val();
                if(checkval==null||checkval==""){
                    alert("请选择您的答案");
                    return;
                }else{
                    //alert(checkval);
                    setCookie("cookiet5",checkval,365);
                    var cookiedata=checkCookie();
                    if(cookiedata.indexOf(",")!=-1){
                        var arr3=cookiedata.split(",");

                        if(arr3.length>0){
                            console.log(arr3);
                            var arrti1=arr3.shift();
                            // alert(arr3);
                            setCookie("arr3key",arr3,365);
                            location.href = "/Yamaha1/" + arr3[0];
                        }
                    }else{
                        alert("您已答完所有题目");
                        location.href="/Yamaha1/subinfo";
                    }
                }
            })
        })
    </script>
</head>
<body>
    <div class="t5wrap">
        <video src="~/Content/yamaha/vedio/t1video.mp4" controls="controls" class="video" width="100%" height="100%" controls poster="~/Content/yamaha/image/videobg.jpg">
            Your browser does not support the video tag.
        </video>
        <p class="title">最后一幕视频场景是在哪拍摄?</p>
        <form>
            <input type="radio" id="t5a" value="A" name="t5"><label for="t5a" class="t5a"></label>
            <input type="radio" id="t5b" value="B" name="t5"><label for="t5b" class="t5b"></label>
            <input type="radio" id="t5c" value="C" name="t5"><label for="t5c" class="t5c"></label>
            <input type="button" class="nextbtn">
        </form>
    </div>
</body>
</html>
